/** @type {import('next').NextConfig} */
const nextConfig = {
  // Должно совпадать с mount path приложения в Webflow Cloud
  basePath: "/gehalts-tool",
  assetPrefix: "/gehalts-tool",
};

export default nextConfig;
